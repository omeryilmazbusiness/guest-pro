kind = "web"
previewPath = "/"
title = "Guest Pro"
version = "1.0.0"
id = "artifacts/guest-pro"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18331

[services.development]
run = "pnpm --filter @workspace/guest-pro run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/guest-pro", "run", "build" ]
publicDir = "artifacts/guest-pro/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18331"
BASE_PATH = "/"
