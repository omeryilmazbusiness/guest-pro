Place the master hero clip here as `bauman.mp4`, then from `artifacts/guest-pro` run:

```bash
pnpm colega:hero-media
```

This generates `videos/bauman-hero.mp4`, `videos/bauman-hero.webm`, and `images/bauman-hero-poster.jpg` (12s loop, web-optimized).
